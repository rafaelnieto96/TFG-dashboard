{% extends "layout.html.twig" %}

{% block title %}Dashboard Homepage{% endblock %}

{% block bodyContent %}
    <div class="container" style="margin-top: 9rem;">
        <div class="row justify-content-center">
            <div class="col-md-3">
                <div class="d-flex flex-column gap-3">
                    <h2 class="section-title text-center">Gamification</h2>
                    <a href="{{ path('events_index') }}" class="btn btn-primary btn-index mb-3">Events</a>
                    <a href="{{ path('user_ranks_index') }}" class="btn btn-primary btn-index mb-3">Ranks</a>
                    <a href="{{ path('achievements_index') }}" class="btn btn-primary btn-index mb-3">Achievements</a>
                    <a href="{{ path('badges_index') }}" class="btn btn-primary btn-index mb-3">Badges</a>
                </div>
            </div>
            <div class="col-md-3">
                <div class="d-flex flex-column gap-3">
                    <h2 class="section-title text-center">Bomberos</h2>
                    <a href="{{ path('posts_index') }}" class="btn btn-primary btn-index mb-3">Noticias</a>
                </div>
            </div>
            <div class="col-md-3">
                <div class="d-flex flex-column gap-3">
                    <h2 class="section-title text-center">Policía</h2>
                    <a href="{{ path('notifications_index') }}" class="btn btn-primary btn-index mb-3">Avisos</a>
                </div>
            </div>
        </div>
    </div>
{% endblock %}
